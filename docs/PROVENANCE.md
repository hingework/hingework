# Provenance

## Owner attestation — recorded verbatim

The GrowBot / local-agent-hub work covered by this extraction was developed as Sergio's personal project, funded personally, and was not commissioned by or created for an employer or client. No employer/client ownership claim is known to apply. This is an owner attestation of fact, not a legal determination — contract review remains a separate, unresolved item gating actual publication.

**PROVEN:** the owner supplied the above attestation in the Segment 2 instruction. Recording it does not independently verify its factual assertions or resolve contracts.

**BLOCKED — actual publication:** contract review remains unresolved. The owner directed inclusion of the MIT license. This is not a legal conclusion drawn from this attestation. Technical readiness does not resolve the separate publication gate.

## Implementation provenance

**PROVEN:** the five extraction components—configuration, packets, providers, council orchestration and records—have documented local implementation lineage. The preceding ownership review identified nine upstream source files at revision `a0e6c095da8947044ea52272b51d1020ff5029cb`, classified each `PROVEN_OURS`, and matched original creation and subsequent development records against Git content.

**INFERRED — review disposition:** all five components are `CLEARED_FOR_EXTRACTION`; no clean-room rewrite is required by the reviewed evidence. No third-party implementation obligation was identified in that lineage. This clearance does not cover unrelated integrations, provider binaries, model weights, or dependency implementations.

**PROVEN — amended component scope:** the selected upstream routing/hash functions introduced in `67465a80463cb43cef6e2f842cf2b1636862defb`, bounded council core introduced in `baab9fa5ae20745b7d08626c1ba52e17e83f1dd9`, and citation-validation helpers originating in `267695a5f5e40dc97bdbe331e784ca89c2d7807a` were independently checked for the escalation amendment. Primary creation/refactor records match the committed source. Their ownership classification is `PROVEN_OURS`; the review disposition is `CLEARED_FOR_EXTRACTION` for the specified generic components, with the amendment's explicit behavioral constraints. This is not blanket clearance for the containing files' project policies or dependencies.

The private audit retains full source mappings, hashes, commit metadata and primary session locations. Do not copy private session logs, absolute source paths, operational evidence, author email addresses or audit reports into the public repository.

**PROVEN — explicit owner-directed exception:** the attestation's person and project names are retained only because the owner explicitly requested this exact text in the provenance file. This is not an exception for source code, examples, templates, the worklog or other operational context.
